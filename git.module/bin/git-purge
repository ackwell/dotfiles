#!/bin/bash

branch=$1
if [[ -z "$branch" ]]; then
	branch="origin"
fi

git remote prune $branch
git branch -r | awk '{print $1}' | egrep -v -f /dev/fd/0 <(git branch -vv | grep $branch) | awk '{print $1}' | xargs git branch -d
