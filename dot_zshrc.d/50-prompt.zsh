# Init the prompt
zinit ice depth=1
zinit light romkatv/powerlevel10k

# Have you _seen_ the size of this file? I 'member when this config was ~10 lines above the install
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh